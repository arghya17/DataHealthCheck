apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-health-check.fullname" . }}
  labels:
    {{- include "data-health-check.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-health-check.selectorLabels" . | nindent 4 }}
